M.block_mydawson = {};
 
M.block_mydawson.init_mydawson= function(Y) {

    /*
    Y.on('change', function(e) {
        var form = Y.one('#mform1'); // The id for the moodle form is automatically set.
        form.submit();
    }, '#id_session');

    Y.on('change', function(e) {
        var parent_el = this.get('parentNode');
        var i = 0;
        var found = false;

        //Dirty hack: keep getting the element's
        //parents until you get to the <form> tag, which has
        //the 'mform' class. Go up a max of 10 levels to avoid an infinite loop.
        //If a themer has the element that deep, they get what they deserve :P
        while (true) {
            if (i >= 10) {
                break;
            }

            if (!parent_el.hasClass('mform')) {
                parent_el = parent_el.get('parentNode');
            }
            else {
                found = true;
                break;
            }
            i++;
        }

        if (found) {
            parent_el.submit();
        }
    }, '.visible_select');
    */
};
